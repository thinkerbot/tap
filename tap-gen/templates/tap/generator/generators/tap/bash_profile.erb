<% if original %><%= File.read(original) %>

<% end %>
# These configurations were added by <%= generator %> on <%= Time.now %><% if install %>
# Your previous '<%= original %>' file was backed up as '<%= backup %>'<% end %>

export TAPFILE=tapfile
export TAP_GEMS=
export TAP_PATH=.
export TAPENV=~/.tapenv:tapenv
export TAPRC=~/.taprc:taprc
export TAP_CACHE=~/.tap
export TAP_DEBUG=false
