###############################################################################
# <%= receiver %> configuration<%= configurations.length > 1 ? 's' : '' %>
###############################################################################
<% configurations.each do |name, default, comment| %>
<%= default == nil ? '#' : ''%><%= yamlize({name => default}) %>

<% end %>

