<% redirect do |target| %># <%= const.name %>::manifest <replace with manifest summary>
# <replace with command line description>

# <%= const.const_name %> Documentation
class <%= const.const_name %> < Tap::Workflow
  protected

  def workflow
	  # Define the workflow entry and exit points,
	  # as well as the workflow logic.
	  self.entry_point = Tap::Task.new
    
	  # app.sequence(entry_point, 'another/task')
  end
end <% module_nest(const.nesting, '  ') { target } end %>