<% tasc = task.class %>
    <span class="class"><%= tasc %></span>: 
    <span class="summary"><%= tasc.manifest.subject %></span>
    <pre><%= tasc.manifest.wrap %></pre>
    
    <ul class="inputs">
<% tasc.args.arguments.each do |argument| %>
    <li class="input">
    <input name="argv[]" value="<%= args.shift %>" />
    <span><%= argument %></span>
    </li>
<% end %>

<% args << '' if task.method(:process).arity < 0 %>
<% args.each do |arg| %>
    <li class="input">
    <input name="argv[]" value="<%= arg %>" />
    <span>...</span>
    </li>
<% end %>
    </ul>
    
<%=  render("configurations.erb", :locals => {
       :configurations => tasc.configurations, 
       :values => task.config, 
       :nest => "", 
       :index => index}) %> 

    <ul class="options">
    <li class="option">
    <input name="argv[]" type="hidden" value="--name" />
    <input name="argv[]" value="<%= task.name %>" />
    <span>Name</span>
    </li>
    </ul>